require.config({
    paths: {
        "jquery": "vendor/jquery",
        "underscore": "vendor/underscore",
        "mustache": "vendor/mustache"
    }
});

require(['jquery', 'src/view'], function($, UI){
//    UI.initialize({rowCount : 6, colCount: 7});

});